{
  "name": "@klickit/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "typecheck": "node -e \"console.log('Tauri shell defers Rust typecheck to tauri build')\"",
    "test": "tsx --test test/**/*.test.ts"
  },
  "dependencies": {
    "@klickit/shared": "0.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
